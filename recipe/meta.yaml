{% set name = "serverengine" %}
{% set version = "2.3.0" %}

package:
  name: rb-{{ name|lower }}
  version: {{ version }}

source:
  url: https://rubygems.org/downloads/{{ name }}-{{ version }}.gem
  sha256: 1263e739820f24e5e066eca0d35953b90f890d6d2f0983148bedf65a6775ddea

build:
  noarch: generic
  number: 0
  script:
    - gem install -N -l -V --norc --ignore-dependencies -n $PREFIX/bin {{ name }}-{{ version }}.gem
    - gem unpack {{ name }}-{{ version }}.gem
  skip: {{ win }}

requirements:
  host:
    - ruby
  run:
    - {{ pin_compatible('ruby', max_pin='x.x') }}
    - rb-sigdump >=0.2.2,<1.0.0

test:
  commands:
    - ruby -r {{ name }} -e 'exit 0'

about:
  home: https://rubygems.org/gems/{{ name }}
  license: Apache-2.0
  license_family: APACHE
  license_file: {{ name }}-{{ version }}/LICENSE
  summary: A framework to implement robust multiprocess servers like Unicorn
  doc_url: https://www.rubydoc.info/gems/{{ name }}
  dev_url: https://github.com/treasure-data/serverengine

extra:
  recipe-maintainers:
    - mxr-conda
    - conda-forge/ruby
